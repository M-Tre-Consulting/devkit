[package]
name = "devkit"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { version = "1.18", features = ["backend-android-activity-06"] }

[build-dependencies]
slint-build = { version = "1.18" }
